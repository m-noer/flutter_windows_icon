import 'package:flutter/widgets.dart';

class WinIconData extends IconData {
  const WinIconData(int codePoint)
      : super(
          codePoint,
          fontFamily: 'WinIcons',
          fontPackage: 'windows_icon',
        );
}
